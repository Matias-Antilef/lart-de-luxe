export enum PublicRoutes {
  REGISTER = "/register",
  LOGIN = "/login",
  CART = "/cart",
  FAVORITES = "/favorites",
  PRODUCT_INFO = "/product-info",
  SEARCH = "/search",
  ACCOUNT = "/account",
}
